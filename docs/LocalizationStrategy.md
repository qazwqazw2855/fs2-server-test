# Localization Strategy

Console localization resources live under `localization/`.

Supported foundation languages:

- `zh-TW`
- `zh-CN`
- `en-US`

Missing selected-language keys fall back to English. Missing English keys return the key name.
